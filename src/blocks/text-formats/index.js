/**
 * Text Formats – RichText format types (Sketch Highlight, Unterstreichen, Marker)
 *
 * Registers two format types for the formatting toolbar in heading and paragraph blocks.
 * Feature must be enabled via GutenBlock Pro Features (text-formats).
 *
 * @package GutenBlockPro
 */

import './circle';
import './marker';

